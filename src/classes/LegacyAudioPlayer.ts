// Already existing code
// Class 2: Legacy class with a different interface
export class LegacyAudioPlayer {
    public playMp3(fileName: string) {
        console.log("Playing mp3 file: " + fileName);
    }
}
